<UserControl x:Class="BgB_TeachingAssistant.Views.PackageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BgB_TeachingAssistant.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="DarkSeaGreen">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <Grid Background="CadetBlue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- For student names -->
                <ColumnDefinition Width="*"/>
                <!-- For grades -->
            </Grid.ColumnDefinitions>
            <!-- Define rows -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Row for the first student -->
                <RowDefinition Height="Auto"/>
                <!-- Row for the second student -->
                <RowDefinition Height="Auto"/>
                <!-- Row for the third student -->
                <!-- Add more rows as needed -->
            </Grid.RowDefinitions>

            <ComboBox Grid.Column="0" Grid.Row="0" Name="StudentComboBox" ItemsSource="{Binding StudentNames}" Width="200" Margin="5"/>
            
            <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Grid.Column="1" Grid.Row="0" Content="&lt;" Margin="5"></Button>
                <TextBox Grid.Column="1" Margin="5" VerticalAlignment="Center">PackageNumber</TextBox>
                <Button  Grid.Column="1" Grid.Row="0" Content="&gt;" Margin="5"></Button>
            </StackPanel>
        </Grid>
        
        <WebBrowser Grid.Column="1" Name="webBrowser" />
    </Grid>
</UserControl>
